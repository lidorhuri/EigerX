select  DEPARTMENT.name, count(EMPLOYEE.id) as employeecnt from
DEPARTMENT left join EMPLOYEE ON DEPARTMENT.id=EMPLOYEE.DEPT_ID
GROUP BY DEPARTMENT.NAME
ORDER BY employeecnt DESC, DEPARTMENT.name asc